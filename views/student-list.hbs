<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Submitted Students</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

  <div class="container py-5">
    <div class="card shadow-sm">
      <div class="card-header bg-primary text-white">
        <h4 class="mb-0">Students Who Submitted the Exam</h4>
      </div>
      <div class="card-body">
        {{#if students.length}}
        <table class="table table-hover align-middle">
          <thead class="table-light">
            <tr>
              <th>#</th>
              <th>Student Name</th>
              <th>View Results</th>
            </tr>
          </thead>
          <tbody>
            {{#each students}}
            <tr>
              <td>{{@index}}</td>
              <td>{{this.student_name}}</td>
              <td>
                <a href="/exam/{{../examId}}/student/{{this.id}}/results" class="btn btn-sm btn-outline-primary">
                  View Results
                </a>
              </td>
            </tr>
            {{/each}}
          </tbody>
        </table>
        {{else}}
          <p class="text-muted">No students have submitted this exam yet.</p>
        {{/if}}
      </div>
    </div>
  </div>

</body>
</html>
